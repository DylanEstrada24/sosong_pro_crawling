
components:
  schemas:
    PlusMemberFileUpload:
      type: object
      required:
        - imageFile
      properties:
        imageFile:
          type: string
          format: binary
          description: 이미지 파일 업로드
          example: 이미지 파일 업로드
